import React from "react";
import axios from "axios";
import { useParams, useNavigate } from "react-router-dom";

const SubtaskActionsEdit = ({ client }) => {
  const { id } = useParams();
  const navigate = useNavigate();

  return <></>;
};

export default SubtaskActionsEdit;
